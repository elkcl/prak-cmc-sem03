use std::io;

fn divmod(c: i32, a: i32, n: i32) -> i32 {
    let mut x: i32;
    let mut y: i32;
}

fn main() {
    let mut n = String::new();
    io::stdin().read_line(&mut n).expect("Input error");
    let n: i32 = n.trim().parse().expect("Input format error");

}
